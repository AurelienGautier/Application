<UserControl x:Class="Application.MeasureTypesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Application"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <ListView x:Name="MeasureTypes" d:ItemsSource="{d:SampleData ItemCount=5}" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Verdana" FontSize="16">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Nom de la mesure" DisplayMemberBinding="{Binding Name}" />
                <GridViewColumn Header="Indice valeur nom." DisplayMemberBinding="{Binding NominalValueIndex}" />
                <GridViewColumn Header="Indice tol+." DisplayMemberBinding="{Binding TolPlusIndex}" />
                <GridViewColumn Header="Indice tol-." DisplayMemberBinding="{Binding TolMinusIndex}" />
                <GridViewColumn Header="Indice valeur" DisplayMemberBinding="{Binding ValueIndex}" />
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
